server {
    listen 127.0.0.1:8080;
    location / {
        root /var/www;
    }
}

server {
    #listen 0.0.0.0:8080;
    listen 10.0.2.15:8080; #d2's valid ip
    location / {
        root /var/www;
    }
}
