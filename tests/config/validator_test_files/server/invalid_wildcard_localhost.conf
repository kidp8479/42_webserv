
server {
    listen 127.0.0.1:8080;
    location / {
        root /var/www;
        methods GET;
    }
}

server {
    listen localhost:8080;
    location / {
        root /var/www;
        methods GET;
    }
}
